module.exports = {
  theme: {
    extend: {
      borderRadius: {
        xl: '32px',
      },
      borderWidth: {
        '12': '12px',
        '16': '16px',
      },
      transitionProperty: {
        inputs:
          'background-color, color, border, border-color, fill, stroke, opacity, box-shadow',
      },
    },
  },
  variants: {},
  plugins: [],
}
